public class Hangman {
    
    public static void main(String[] args) {
    // hardcoded for now - should take input from non-play via prompt?
      Game game = new Game("longdongsilver");
      Prompter prompter = new Prompter(game);
      prompter.play();
    }
}
